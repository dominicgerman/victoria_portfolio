type Props = {
  context: string[]
  sprintDetails?: string
  outcomes?: {
    business: string[]
    product: string[]
  }
}

export default function Description({}: Props) {
  return <div>Description</div>
}
